{
  "name": "puzzle-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Game-agnostic multiplayer puzzle platform (first game: Chain Reaction)",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/games/*",
    "apps/web"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --noEmit",
    "qa": "tsc --build && vitest run",
    "web:dev": "npm -w apps/web run dev",
    "web:build": "npm -w apps/web run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "happy-dom": "^20.10.6",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
